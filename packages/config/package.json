{
  "name": "@wspa/config",
  "version": "0.0.4",
  "description": "WSPA configs",
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "watch": "tsc -b -w"
  },
  "dependencies": {
    "@wspa/cli": "workspace:*"
  },
  "publishConfig": {
    "directory": "lib"
  },
  "engine": {
    "node": ">=16.0.0"
  }
}

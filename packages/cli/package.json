{
  "name": "@wspa/cli",
  "version": "0.0.3",
  "description": "CLI tool for creating workspaces",
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "watch": "tsc -b -w"
  },
  "dependencies": {
    "@pnpm/find-workspace-packages": "^4.0.2",
    "@pnpm/logger": "^4.0.0",
    "commander": "^9.2.0",
    "cpy": "^9.0.1",
    "fast-glob": "^3.2.11",
    "json5": "^2.2.1",
    "tsconfig": "^7.0.0",
    "yaml": "^2.0.1"
  },
  "publishConfig": {
    "directory": "lib"
  },
  "engine": {
    "node": ">=16.0.0"
  },
  "x-wspa": {
    "pkgManifest": {
      "bin": {
        "wspa": "bin.js"
      }
    }
  }
}
